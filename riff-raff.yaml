stacks: [mobile]
regions: [eu-west-1]

deployments:
  mobile-save-for-later:
    type: aws-lambda
    parameters:
      bucket: mobile-dist
      functionNames: [mobile-save-for-later-]
      fileName: mobile-save-for-later.jar
      prefixStack: false
